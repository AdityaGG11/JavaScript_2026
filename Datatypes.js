// We have the standard primitive datatypes and then objects
/*
Things to remember:-
When we check the type of null, we get object
When we check the type of undefined, we get undefined
This is treated as an error of the language by some and just a feature by others so leave it
*/
let var1 = 1234
let var2 = "Aditya"
let var3 = true
console.table([typeof var1, typeof var2, typeof var3])
//This will print a table specifying the datatypes of different variables
